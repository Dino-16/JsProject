// Output a friendly greeting to the console
console.log("Hi there!");
console.log("I am excited to talk to you.");

// Declare a variable named 'bot' to store the bot's name
let bot;

// Assign an initial name to the bot
bot = "teacherBot";

// Declare and initialize the bot's location
let botLocation = "the universe";

// Introduce the bot with a message
console.log("Allow me to introduce myself.");

// Create a sentence using string concatenation to introduce the bot's name
const botIntroduction = "My name is " + bot + ".";
console.log(botIntroduction);

// Create a sentence using string concatenation to share the bot's location
const botLocationSentence = "I live in " + botLocation + ".";
console.log(botLocationSentence);

// Update the bot's name to a new nickname
bot = "professorBot";

// Create a sentence introducing the bot's nickname
const nicknameIntroduction = "My nickname is " + bot + ".";
console.log(nicknameIntroduction);

// Update the bot's name again to a more expressive nickname
bot = "awesomeTeacherBot";

// Create a sentence expressing the bot's preferred nickname
const newNicknameGreeting = "I love my nickname but I wish people would call me " + bot + ".";
console.log(newNicknameGreeting);

// Declare and initialize the bot's favorite subject
const favoriteSubject = "Computer Science";

// Create a sentence sharing the bot's favorite subject
const favoriteSubjectSentence = "My favorite subject is " + favoriteSubject + ".";
console.log(favoriteSubjectSentence);

// End the conversation with a polite farewell
console.log("Well, it was nice to talk to you. Have a nice day!");